# Samar Cake Website

React and Vite website for Samar Cake.

## Local preview

```bash
pnpm install
pnpm dev
```

## GitHub Pages

Upload this cleaned project to a GitHub repository, then enable GitHub Pages with **Source: GitHub Actions**.

Every push to the `main` branch will build the website and publish the finished `dist` output automatically.
